# 验证报告: 知识库向量检索维度不匹配

**日期**: 2026-06-06 | **缺陷**: #001-vector-dim-mismatch

## 验证环境

- **操作系统**: macOS Darwin 24.5.0
- **Python**: 3.13.7
- **Friday**: cd477a1 (修复后)
- **验证方式**: 模拟原始 bug 复现步骤

## 修复有效性

### 测试 1: 知识库向量搜索（原始 bug 场景）

```
搜索 '知识库内容' → Collection 无维度标记，自动重建 → 返回 5 条结果
```

**结论**: ✓ 修复有效。旧 1024 维集合被自动检测并重建为 512 维。

### 测试 2: Mem0 probe 搜索

```
Mem0 初始化 → probe search 正常 → 无维度错误
```

**结论**: ✓ Mem0 初始化和 probe 正常。

### 测试 3: 新笔记向量索引 + 语义搜索

```
添加笔记 '验证测试笔记' → 向量搜索 '验证向量检索修复' → 返回 1 条结果 (score=0.879)
```

**结论**: ✓ 写入和语义搜索均正常。

## 回归检查

| 检查项 | 结果 |
|--------|------|
| FTS5 全文搜索 | ✓ 正常返回 |
| 笔记添加/删除 | ✓ 正常 |
| Mem0 初始化 | ✓ 正常 |
| adapter 降级逻辑 | ✓ 未被修改，保留原样 |
| embedding 模块 | ✓ 未被修改 |

## 代码质量检查

| 检查项 | 结果 |
|--------|------|
| 文件大小 | ✓ vector.py 113行, dynamic.py 195行 |
| 函数大小 | ⚠ `_create_mem0_instance` 34行（超限4行） |
| 类型注解 | ✓ 完整 |
| 安全隐患 | ✓ 无（shutil.rmtree 仅在维度不匹配时清理 Friday 数据目录） |
| 冗余代码 | ✓ 无 |

### 已知问题

- `_create_mem0_instance()` 34 行，超 30 行限制 4 行。原因为 Mem0 config dict 占 16 行。提取此函数是为了 probe 失败后重试创建，属于修复必要改动。

## 验证结论

**修复有效**。知识库向量检索和 Mem0 的维度不匹配问题已解决，自动重建机制工作正常，无回归问题。
